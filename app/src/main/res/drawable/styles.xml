<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <resources>
        <!-- Base application theme -->
        <style android:name="AppTheme" android:parent="Theme.AppCompat.Light.DarkActionBar">
            <!-- Customize your theme here -->
            <item android:name="colorPrimary">@color/colorPrimary</item>
            <item android:name="colorPrimaryDark">@color/colorPrimaryDark</item>
            <item android:name="colorAccent">@color/colorAccent</item>
        </style>

        <style android:name="GrayButton">
            <item android:name="android:background">@color/button_gray</item>
        </style>
    </resources>
</selector>